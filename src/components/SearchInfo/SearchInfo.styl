.keyword-list-wrapper {
  color: #8b8b8b;
  .title {
    font-size: .6rem;
  }
  .keyword-list {
    // todo
    list-style none
    margin 0
    padding 0
    .keyword-list-item {
      display: inline-block;
      height: 1.5rem;
      line-height: 1.5rem;
      font-size: .6rem;
      padding: 0 1rem;
      margin-right: .6rem;
      margin-bottom: .3rem;
      border-radius: .2rem;
      background-color: #f0f0f0;
    }
  }
}

.search-info-wrapper {
  padding 0 1.5rem 1rem
}