.float-button {
  position: absolute;
  bottom: 1rem;
  right: 1rem;
  display: block;
  border: none;
  width: 5rem;
  height: 5rem;
  transition: all .5s;
  background-color: transparent;
  background-image: url(../../assets/comment-float-button.png);
  background-size: 100% auto;
}